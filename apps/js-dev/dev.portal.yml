name: dev.js
package: portal.dev.js
title: Portal JS
description: Portal JS development runner
hidden: true
build:
  exec: go run
  default:
    out: main
    cmd: go build -o $OUT
  windows:
    out: main.exe
    cmd: go build -o $OUT.exe
