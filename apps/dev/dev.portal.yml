name: dev
package: dev.portal
title: Portal Dev
description: Portal development service.
hidden: true
schema: go
build:
  exec: go run
  default:
    out: main
    cmd: go build -o $OUT
  windows:
    out: main.exe
    cmd: go build -o $OUT.exe
