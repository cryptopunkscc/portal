name: dev
package: dev.portal
title: Portal Dev
description: Portal development service.
hidden: true
schema: go
exec: go run
build:
  default:
    out: main
    cmd: go build -o $OUT
  windows:
    out: main.exe
    cmd: go build -o $OUT.exe
