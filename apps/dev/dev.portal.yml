name: dev
package: dev.portal
title: Portal Dev
description: Portal development service.
version: 0.0.0
hidden: true
schema: go
exec: go run
build:
  default:
    out: main
    cmd: go build -o dist/main
  windows:
    out: main.exe
    cmd: go build -o dist/main.exe
