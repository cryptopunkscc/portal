name: apps
package: portal.apps
title: Portal apps
description: Portal apps management
version: 0.0.0
schema: go
build:
  default:
    exec: main
    cmd: go build -o dist/main
  windows:
    exec: main.exe
    cmd: go build -o dist/main.exe
