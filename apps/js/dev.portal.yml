name: js
package: portal.js
title: Portal JS
description: Portal JS runner
hidden: true
schema: go
exec: go run
build:
  default:
    out: main
    cmd: go build -o $OUT
  windows:
    out: main.exe
    cmd: go build -o $OUT.exe
