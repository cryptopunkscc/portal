name: js
package: portal.js
title: Portal JS
description: Portal JS runner
version: 0.0.0
hidden: true
schema: go
exec: go run
build:
  default:
    out: main
    cmd: go build -o dist/main
  windows:
    out: main.exe
    cmd: go build -o dist/main.exe
  linux_x86:
  linux_x64:
