name: js
package: portal.js
title: Portal JS
description: Portal JS runner
hidden: true
schema: go
build:
  exec: go run
  default:
    out: main
    cmd: go build -o $OUT
  windows:
    out: main.exe
    cmd: go build -o $OUT.exe
