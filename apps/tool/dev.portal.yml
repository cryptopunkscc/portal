name: tool
package: portal.tool
title: Portal tools
description: Various portal tools.
schema: go
build:
  exec: go run
  default:
    out: main
    cmd: go build -o $OUT
  windows:
    out: main.exe
    cmd: go build -o $OUT.exe
