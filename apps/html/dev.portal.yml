name: html
package: portal.html
title: Portal HTML
description: Portal HTML runner
version: 0.0.0
schema: go
build:
  default:
    exec: main
    cmd: go build -o dist/main
  windows:
    exec: main.exe
    cmd: go build dist/main.exe
