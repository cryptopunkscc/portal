{
  "name": "portal-example-backend",
  "version": "0.0.1",
  "description": "Example Portal backend app",
  "scripts": {
    "build": "rollup index.js -o dist/main.js -f iife" ,
    "watch": "rollup index.js -o dist/main.js -f iife --watch"
  },
  "devDependencies": {
    "rollup": "^4.12.0"
  }
}
