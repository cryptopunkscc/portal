name: project
package: new.portal.project
version: 0.0.0
title: project
author: yang
email: cryptopunkyang@pm.me
build:
  default:
    deps: [ dep2 ]
    cmd: cmd2
  linux:
    deps: [ dep3 ]
    cmd: cmd3
  windows:
    deps: [ dep4 ]
    cmd: cmd4
