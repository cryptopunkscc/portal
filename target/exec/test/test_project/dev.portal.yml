name: name
package: package
title: title
description: description
version: 1
schema: schema
build:
  out: out
  cmd: cmd
  linux:
    exec: exec
    deps: [ dep ]
    env: [ DEP=1 ]
  windows:
    exec: exec.cmd
